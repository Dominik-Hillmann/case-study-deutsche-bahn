/**
 * Every class that is concerned with telling the users something about the
 * inner workings of the API.
 * For example, that there is no other API other than /api/betriebsstellen when
 * calling /api/ only.
 */
package com.bahn.casestudy.info;